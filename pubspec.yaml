name: flutter_object_detection_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hooks_riverpod: ">=0.12.0 <1.0.0"
  flutter_hooks: ">=0.14.0 <1.0.0"
  tflite_flutter: ">=0.5.0 <1.0.0"
  tflite_flutter_helper: ">=0.1.2 <1.0.0"
  simple_logger: ">=1.7.0 <2.0.0"
  camera: ">=0.5.8 <1.0.0"
  flutter_screenutil: ">=3.2.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: any

flutter:
  uses-material-design: true
  assets:
    - assets/
